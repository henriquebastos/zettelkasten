{
  "subagentStatusLine": {
    "type": "command",
    "command": "node --eval 'const fs=require(\"node:fs\"),path=require(\"node:path\"),chunks=[];const valid=value=>{if(!/^[1-9]\\d*(?:[a-z]+[1-9]\\d*)*(?:[a-z]+)?$/.test(value))return false;for(const segment of value.match(/\\d+|[a-z]+/g)??[]){let number=0;if(segment[0]>=\"0\"&&segment[0]<=\"9\")number=Number(segment);else for(const character of segment)number=number*26+character.charCodeAt(0)-96;if(!Number.isSafeInteger(number)||number<1)return false}return true};process.stdin.on(\"data\",chunk=>chunks.push(chunk));process.stdin.on(\"end\",()=>{let input;try{input=JSON.parse(Buffer.concat(chunks))}catch{return}const xdg=process.env.XDG_CACHE_HOME,home=process.env.HOME,base=xdg&&path.isAbsolute(xdg)?xdg:home&&path.isAbsolute(home)?path.join(home,\".cache\"):undefined;if(!base)return;for(const task of input.tasks??[]){if(!task.id)continue;let address;try{address=fs.readFileSync(path.join(base,\"zettelkasten\",\"claude-code\",\"assignments\",Buffer.from(task.id).toString(\"base64url\")),\"utf8\").trim()}catch{continue}if(!valid(address))continue;const label=task.label?.trim()||task.name?.trim()||task.description?.trim();process.stdout.write(JSON.stringify({id:task.id,content:label?`${address} ${label}`:address})+\"\\n\")}})'"
  }
}
