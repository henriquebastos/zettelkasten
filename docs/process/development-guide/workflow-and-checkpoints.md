# Workflow and Checkpoints

Canonical taxonomy, work-area routing, expansion/collapse, lifecycle, and checkpoints live in the installed skill references—especially `method/work-areas`, `method/expand-collapse`, `delivery/roadmap-taxonomy`, `delivery/story-lifecycle`, and `delivery/checkpoints`. Load them through `using-ariad/SKILL.md`; if skill discovery is unavailable, open the installed `SKILL.md` manually and follow its reference routing.

This file records **only explicit project adaptations**. An empty section means the canonical packaged guidance applies; it does not freeze the defaults that existed when these templates were copied.

## Local Taxonomy, State, or Routing Adaptations

- None currently.

## Local Lifecycle and Checkpoint Adaptations

- None currently.

## Refinement State Owner

- Not configured. Before active Refinement begins, replace this line with one authorized owner: a repository Workbench path, an external issue tracker/project surface, or another project-owned system. Without that owner, Memory Closure must mark context release not safe when active Refinement state would otherwise exist only in conversation.

## Local Debt Review Policy

During review, name debt paid, introduced, and carried forward; its revisit trigger; and whether `docs/project/debt/items/` needs a durable item. Small local debt may remain follow-up work, but debt affecting future delivery, safety, maintainability, validation, operation, or product coherence belongs in the ledger.
